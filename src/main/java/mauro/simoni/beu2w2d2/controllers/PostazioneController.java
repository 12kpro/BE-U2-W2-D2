package mauro.simoni.beu2w2d2.controllers;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/postazioni")
public class PostazioneController {
//    @Autowired
//    PostazioneSrv postazioneSrv;
////    @GetMapping("/{postazione}/{citta}")
////    public Postazione getPostazioneCitta(@PathVariable String p, @PathVariable String c){
////        return postazioneSrv.getPostazione(p,c).orElseThrow(() -> new ReqException(p,c));
////    }
}
